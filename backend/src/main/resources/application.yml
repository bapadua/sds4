
spring:
  profiles:
    active: test

---

spring:
  profiles: test

  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2

  jpa:
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        format_sql: true

---


